fos_rest:
    routing_loader: false
    view:
        view_response_listener: true
    format_listener:
        rules:
            - { path: ^/v1/api, prefer_extension: true, fallback_format: json, priorities: [json] }
            - { path: ^/, fallback_format: html, priorities: [html] }
